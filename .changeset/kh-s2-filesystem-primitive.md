---
"@kuralle-agents/fs": minor
"@kuralle-agents/core": minor
---

Add `@kuralle-agents/fs` portable filesystem primitive with `InMemoryFs`, `createFsTool` (`workspace` tool), and `AgentConfig.workspace` auto-registration. Expose `RunContext.fs` for out-of-band flow actions.
