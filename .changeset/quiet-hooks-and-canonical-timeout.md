---
"@kuralle-agents/core": minor
---

Isolate failures from lifecycle hooks so user callbacks cannot change run correctness, and remove the unused `@kuralle-agents/core/tools` error-handling exports in favor of the canonical effect error surface.
