---
"@kuralle-agents/core": minor
---

Rename `AgentConfig.effectTools` to `tools` (durable effect tools); remove raw `ToolSet` agent field; add `wrapAiSdkTool()` for AI SDK interop; journal-route host reply tools; add CI guard `check-no-raw-tool-execute.sh`.
