---
'@kuralle-agents/core': minor
---

Wire `agent.memory.workingMemory` into the runtime (block load, prompt injection, `memory_block` tool). Workspace defaults to read-only; read-write workspaces are not auto-exposed in `globalTools`.
