---
"@kuralle-agents/rag": minor
"@kuralle-agents/core": patch
---

Add `KnowledgeFs` — a read-only `FileSystem` adapter over `VectorStoreCore` with chunk reassembly, RBAC tree-pruning, two-stage grep via optional `fs.search`, and a support-kb agent example.
