# Transcript: https://www.youtube.com/watch?v=sIHMOd0awFc
# Source: youtube-captions
Hey everyone. In this video, we're going to take a look at coding agent under the hood and try to understand how it actually works. The first time you see an LLM, not just answering questions, but running commands and making changes across a codebase that actually work, it feels like a huge shift. And thanks to open source and the great work from the open code team, we get to explore that in great detail. So, let's dive in. So, the basic workflow is as follows. You install your coding agent. Uh, in my case, a brew install does the trick. Then you configure your API key so the agent can interact with an LLM. Uh the cool thing about open code is it's provider agnostic and it supports a bunch of models even local ones as long as they have an open AI compatible API. So that's pretty cool. So we do that as such and and we provide the API key and then once you've done that you just start the open code command inside your project's repository. So in my case I cloned the open code repo and I'm running open code inside the open code repo. So we got the little inception thing going on. So once you run the command you're faced with this tui this text user interface and you basically ask the agents to do things. You can ask it to analyze the code to build a new feature to fix a bug. In my case I'm going to ask it to transform this and make sure it does not make any mistakes anyway. So while doing this the code has access to tools so it can read files, modify files, run commands and all that. So how does it all work? What are we exactly trying to accomplish? So we have a project that contains several code files and we want to interact with a large language model or an agent and have it uh modify these files, read their contents, analyze uh the code and perform various actions. So the key uh principle is having the LLM interact with our files. And the way we do that is by providing the LLM with tools. So the way this works is when we call a model. So this is for instance the enthropic uh documentation. We can provide it with a list of tools. So here we give the get weather tool its description its input schema and if you check out the open AI documentation it has a similar construct uh get weather tool description and the parameter and these models can do more than just calling the get weather tool. The model companies are apparently fascinated with the weather anyway. So we give the model a list of tools. uh we describe what the tool does uh what's uh what par parameters it's expecting and then when the model uh deems the tool call appropriate and uh relevant to the current task or to the current prompt it will actually output the name of that tool and the runtime environment so the code that's actually calling the LLM will execute that uh tool call so the developer er wants to make changes to a project. So they start the open code agent and they are faced with the front end the chewy the text user interface. The developer submits a prompt which is sent from the TUI to the back end. So the processing can actually take place. So open code is architectured as a server client application with uh the TUI being the client communicating with the server over HTTP. uh the application also has an event bus. So whenever something meaningful happens in the back end, it gets published to this event bus and you can have uh events published to the event bus. But you can have also different parts of the application that subscribe to the event bus into certain events. So they get notified of these events. Uh the HTTP server also exposes uh slashevent endpoint HTTP endpoint that HTTP clients can uh connect to. So the tui the front end connects to this /event endpoint. So when I started my uh open code uh agent uh an http server in this uh was started in this port and when I connect to the slashevent endpoint I get this. So if I type something into the TUI now for instance events get sent in the SSC endpoint and so the way it looks in the code so there is this / SSC uh SLevent endpoint which is uh which has a content type of event stream and it subscribes to all the events that have that are published to event bus and then they are streamed back to the clients to HTTP clients. So what this means what this means is that uh if this if the client sends a prompt and the the work takes a long time. So the client is going to be updated as events happen in the back end. So it does not have to wait until things finish but it's g it's going to see the updates as they happen because they are published from the event bus to this uh SSC uh endpoint and that's pretty cool when you think about it and the Golang Tuy uh will listens to these events and as they happen it updates the TUI and when there is a a permission uh that needs to be granted it shows it to the user when there is an error as well and so So having access to tools and to the local environment is what differentiates a useful coding agent capable of accomplishing tasks from a vanilla LLM. Open code comes with a list of built-in tools uh very much similar to the actions that a developer needs to take in their day-to-day. So you have a bash tool to run bash bash commands to inspect files or move files or copy them. You have an edit tool to change the content of the file. You can read files. You can create a to-do list. You can fetch content from uh the web so you can look up information in some documentation and a lot more. In addition to these built-in tools, uh Open Code uh allows the ability to plug in MCP servers. So having the ability to add an MCP server uh either locally or a remote one and then uh load up the tools and the resources this MCP server has to offer is a pretty powerful capability and this means you can easily extend the capabilities of your coding agent. So so let's explore the tools to make things a little bit more concrete. So I had uh a session with uh open code and I actually wanted to make some changes to open code itself. So as we previously described currently open code has a twoe client. So that's a text user interface in the terminal and I and since it has an a client server HTTP model I thought it would be fun to build an HTTP client that's going to call the open code back end from the browser. And so I started this session to add a new endpoint to the HTTP server that will serve a client that would interact with open code through the from the browser. And now when I connect to the HTTP port exposed by the open code server and go to the client endpoint, I can actually see an HTT an HTML client which is pretty cool. And after a few prompts, it was actually capable of generating a semi working example, which is pretty impressive. I think I was using gro code. So anyway, so let's take a look at the tools. Maybe we can explore the conversation a little bit first. So I asked it to add new HTML server endpoint in the server.ts file. It prompted me for my permission to allow this change and I approved. So it called the edit tool and then I was not too happy. It had some errors. So I provided the error trace and asked it to fix it and then I asked it to improve the style and it had some linting errors and all that and at the end it was actually capable of uh providing a working example that's capable of listing the sessions. So a session is basically a conversation between a client and the the backend server and when you start a new session you you start fresh from new with a fresh context window. So when you click on uh a session it fetches the messages within that session and you can even send which is pretty pretty cool. So anyway let's take a look at the retool. So a tool definition is rather simple. So we need to have the description of the tool and this is basically uh informing the LLM of what's of the capabilities of this tool. So the description of the read tool is reads a file from the local file system. You can access the file blah blah blah and you can uh and it has an offset parameter. So for for very big files, you don't want to read the whole file because you're going to saturate your context window. So you want to be able to read uh with a limit and sometimes when you have an error in specifi in specific line you don't want to read the whole file but start from that specific or before just before the that line. So the read tool has these two knobs that can be tweaked by the LLM the offset and the limit and it's not as simple as reading the file. You need to make sure that the model is not accessing the file outside of the current working directory. Otherwise, this could be malicious or harmful. We need to check for images and binary files. So, we're not reading uh incomprehensible data and some other checks. And finally, we give the output of the read operation to the model. So, in op code, we have a session that's the whole conversation. Each within the session, we have messages and each message is composed of chunks or parts. So what we're looking at here is a part of a message which is uh a tool uh response. So we can see uh type tool and the status of the call is completed and this is a read operation. So when I asked my agent to make changes to the server.ts it the the LLM decided that it needed to read the contents of that file. And so the LLM uh generated a tool call and then the JavaScript back end the AI SDK called the execute function of the read tool. That execute function actually read the file from the file system and uh added it to the context of the LLM and the LLM is now able to read the file and that's what happened. And we can see here that the LLM wanted to read starting from uh line 150 and read only 30 lines. This was not the first call. I believe this was uh at one of the iterations. But it gives a sense of how it actually works under the hood. So that was the read tool. The bash tool is very similar. We define a tool. We provide its description. So the bashful description uh goes like this. Execute a given bash command in a persistent self session. And then we specify information about how the path should look like about uh how to call certain commands. That's the description. And then running the command. So that's actually the execute command. We can see that the bash command sometimes requires permissions. And so if uh the user's permission is needed, we ask for it and basically the user is prompted in the terminal. Do they ex do they accept or deny the permission? And if it's granted, then we execute the command and we get this process variable. Then we take the standard output of that process and the standard error add it to the result of the execute function and we return that to the LLM and that's added to the LLM. So the bash command is executed and the output of that execution is added to the context. For me the to-do tool is one of these things that when you see for the first time you are in that wow effect moment and seeing a coding agent create a to-do list and they starting to cross off items from the list was really quite impactful. So anyway, the way open code uh implements the to-do tool is uh as always we have our uh description and there are two tools within to-do. So we have the to-do write in which we update the to-dos and the to-do read in which we read the actual to-do. The L&M provides a list of to-do infos to the tool and the tool simply has a global map of to-dos and it uh for each session ID it assigns a list of to-dos. So the LSP tools is in my opinion one of the most powerful tools the agent has access to. So LSP language server protocol is what allows us to have uh information in our IDE about our current project. So when we add a variable name that does not exist or we make some uh syntax error or whatever we see this squiggly red line in the in the IDE or warning or whatever and that's thanks to LSPs and the diagnostic message from LSP looks something like this. So for instance, if we have uh a variable that's not defined that we are using the LSP client, our editor would uh get a message from the LSP server as this and it says that this variable is not defined. So imagine this. Each time the coding agent makes changes to the code to the to to a a code file, the uh LSP server uh checks these changes and if it does not adhere to the language rules or if there's something wrong with it, it emits a warning. that warning is added to the LLM's context and so it can see in real time if its changes make sense or don't and uh the way open code implements this. So uh when when it starts it launches an LSP server for various programming languages. So for Python it launches a py pyrite server and for Golang it has a go pls server it has a ruby l uh lsp server and so on and so forth and the lsp server and client communicate through standard IO uh using JSON RPC. So the messages between the LSP client in our case it's an embedded client within our uh in our case it's embedded client with their own coding agent. It communicates with the created LSP server using standard IO. And so when the edit tool is uh called within it we have uh a call to the LSP server to get the diagnostics. And it looks something like this in the code. Here we are within the edit tool function. And after we write our changes, the new content, we get the diagnostics from the LSP server and add these to the LLM context. And what does this look like? So in terminal interface we actually see the errors as part of the edit tool. Once the tool the edit tool is called and the LSP diagnostics uh have been retrieved the LLM sees in its output this file has errors and then we see the exact errors as as output by the LSP server which is pretty cool and I think this is one of the reasons why coding agents uh are a great application of LLMs because there are or there is already so much tooling in many guard rails. So if the LLM makes up something nonsensical or hallucinates, the LSP and other tools are there to to keep it in check. And one other thing the LLM can do is it can run tests. So after it makes some changes, it it runs npm run test and then if there is a failure with the test that can be seen within the within the output and the model can adjust accordingly. Many people like to say that agents are simply LLMs in a loop with access to tools. This is what I like to call the $1 trillion loop. So where is our $1 trillion loop? So we have a system prompt. We have defined tools. Now we actually need to call the LLM with these tools and act upon the response. So the way open code does this when a user submits a prompt in their TUI or in a different client this goes to the as a message that is created via the HTTP endpoint and then we call the session.prompt function within that function we bring in the various tools and the system prompts and all that. Then we call the AISDK stream text function. So the stream text function provided by the AISDK and which is of course provider agnostic streams the models result as they are happening. So we don't need to wait for the whole generation to finish. We can see it uh as it is progressing and uh behind the under the hood it's implemented using JavaScript generators and it's a pretty cool implementation. But anyway uh as uh these uh stream text results are arriving our back end our JavaScript back end is handling the responses. So whenever there is a tool call the emitted by the LLM the back end actually calls that tool when there is a and and gives the tool result back to the to the LLM and it keeps progressing and in our case the as these events are happening they are also streamed uh from the HTTP back end to the front end to the two client and to any other client which that is subscribed to the SLevent endpoint and so this is added it's hard the coding agent process. So we submit the prompt uh we define the tools and the system prompt and the user prompt and then this is sent back to a model using the stream text function and as the results are uh received from the LLM. We process these results. If it's a tool call, we actually call that tool and provide the result of the the tool call back and it keeps progressing. If we need a permission for a certain tool call as it's defined within the within the open code configuration, we ask the user for their permission and it keeps happening which is pretty cool. The processing loop uh looks something like this. So depending on the type of the uh the stream response item, it could be a tool call, it could be a tool result, a tool error, for instance, if it's tool error, we decide that we should stop this uh stream. And uh if it's text delta and so on and so forth and one cool thing, it's that uh the stream text function is capable of making multiple tool calls. And so each tool call is considered a step and we can specify within the stream text parameters that we want to stop after a certain number of steps. So here for instance we say stop after a thousand steps which seems like a lot or if a certain condition is satisfied in our case we should stop if there is an error. And so before each after each step starts, what the open code processor does is it takes a snapshot. And what this snapshot does is it takes all the change file in the git repository and dumps the changes into a snap snapshot directory using this track function. So we add the current files to the uh change files to the index and then we write the tree and if there is an error or we want to revert for whatever reason we just read the snapshot and we retrieve our previous state and this is very much similar to what uh a commit does but this does it without actually authoring the commit history. When open code interacts with an LLM it does that within the context of an agent. So an agent is an LLM plus uh an additional agent specific system prompt plus uh a set of tools. So we have the plan the plan agent which uh is uh used to plan task and analyze a problem and outline a plan. We have the build agent which uh relies on a plan or can figure out the problem itself and actually make changes. Whereas the plan agent does not have access to edit tools, the build agent does. And we can define our own agent. So we can uh define a review uh an agent that reviews code. We can define define an agent that specializes in security. And each agent can have its own model, its own tools and its own temperature and all that. So there is a very interesting tool called the task tool that a primary agent has access to. So the task tools definition reads description reads like this. Uh this is uh a tool. So a task tool is a tool like any other and its description says that you have access to certain agent types and these are the sub aents that are defined by the user or within open code and whenever there is a task that's an independent task for instance if we ask our primary agent the the build agent to check for security vulnerabilities and we have defined a security sub agent the primary build agent is going to make use of the task tool to call that sub agent and that sub agent will have its own contact uh uh context window with potentially a different model and it's going to run and uh call its own tools and it's done it's going to return to the primary agent and this is just one sub agent we could have multiple sub agents so if we ask the Allen to review the primary agent to review the code for vulnerabilities but also for a regular code code review it could spawn two sub aents which are basically two tool calls they can run on their own LLM contacts and then they return and having these sub agents is what creates the whole agentic workflows. It's pretty interesting. So the way developer interacts with open code is through tui a text user interface which is beautifully designed and rather smooth and it's written in golang. One thing that intrigued me when I was exploring open code that how they made the Golang 2 uh play nice with the JavaScript back end and how they wire the two together and it's rather nifty. So the way they do it is they build the uh Golang binary D2 uh into a binary file and then that file is imported into uh the JavaScript code and bun has this cool command uh bun build compile which basically uh builds your application and it packages with its dependencies and the bun runtime into a standalone binary. So we have a single binary that has the bun dependencies, the bun runtime, but also the two embedded. So when a user runs the open code command, they are actually running the bun standalone binary. The binary will start an HTTP server. Then it's going to look within its embedded files for the 2e binary. That 2-ye binary then is started. So we spawn a new process using the 2e binary and that process inherits the standard output and standard input of the bun process. So when we interact with the command line we are actually we interact with the terminal we are actually interacting with the two uh process and within the golang uh startup method. So this is uh the main function of the Golang uh binary and within it we have this uh HTTP client that's going to call the list streaming uh method. What this method does basically it's going to subscribe to the SLevent endpoint that we saw at the beginning. And so each time an event happens in the back end, it's going to be streamed from the HTTP server to the to HTTP client. So where do we go from here? Uh exploring the code has been really interesting and I believe we barely scratched the surface. Uh one takeaway for me is that it's really hard to build uh a good product with a good developer experience and a well architectured app. So, kudos to the open code team. Uh, I hope this has been informative. Thank you very much for watching. Bye-bye.
