# Artifact lifecycle

Kuralle hashes the authored files, publishes an immutable agent version, activates a release for new
threads, and keeps an existing thread on its original version through a durable pin.
