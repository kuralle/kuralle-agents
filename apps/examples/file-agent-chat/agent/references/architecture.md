# Example architecture

The generated Node server resolves an immutable agent version, runtime revision, release, and sticky
thread pin before running a turn. The Kuralle CLI addresses the deployed agent entity and thread.
