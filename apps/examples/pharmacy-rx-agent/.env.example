# Vercel's server route forwards to the durable Cloudflare agent.
CLOUDFLARE_AGENT_URL=https://kuralle-pharmacy-workspace-agent.example.workers.dev

# Required only by the Cloudflare Worker / direct local Node runtime.
OPENAI_API_KEY=
MODEL_ID=gpt-4.1-mini
