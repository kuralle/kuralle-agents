# WebSocket Support Example

Runs a customer support voice agent over plain WebSocket transport.

## Run

```bash
bun run --cwd apps/playground/transport-examples websocket-support
```

## Endpoint

- WebSocket server: `ws://localhost:8080`
