# Twilio Support Example

Runs customer support voice agent via Twilio Media Streams.

## Run

```bash
bun run --cwd apps/playground/transport-examples twilio-support
```

## Notes

Configure Twilio `<Stream>` to point at your deployed `wss://.../twilio/media` endpoint.
