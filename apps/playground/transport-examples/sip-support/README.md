# SIP Support Example

Runs customer support voice agent over SIP/RTP (UDP).

## Run

```bash
bun run --cwd apps/playground/transport-examples sip-support
```

## Defaults

- SIP bind: `127.0.0.1:5060`
- RTP start port: `10000`
