# LiveKit Cloud — https://cloud.livekit.io
# `lk app env -w -d .env.local` fills these in for you.
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Model provider for the Kuralle agent brain (gpt-4o-mini by default — see src/runtime.ts).
OPENAI_API_KEY=
