# AI Provider
OPENAI_API_KEY=your-openai-api-key

# Vector Store Selection: "memory" (default), "pg", or "redis"
VECTOR_STORE=memory

# PostgreSQL (required when VECTOR_STORE=pg)
# DATABASE_URL=postgresql://localhost:5432/kuralle_rag

# Redis (required when VECTOR_STORE=redis)
# REDIS_URL=redis://localhost:6379
