{
  "$schema": "https://agent-plugins.org/schemas/1.0.0/plugin.schema.json",
  "name": "symlink-inside-plugin",
  "description": "Ships a bin/ symlink that stays inside the plugin root, which §4.1(3) permits."
}
