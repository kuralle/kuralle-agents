{
  "name": "@kuralle-agents/eval",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kuralle/kuralle-agents.git",
    "directory": "packages/kuralle-eval"
  },
  "version": "0.3.19",
  "description": "Deterministic conversation replay and assertions for Kuralle transcripts",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "node --test test/*.test.js",
    "test:golden": "tsc -p tsconfig.json && node dist/golden.js",
    "prepublishOnly": "tsc -p tsconfig.json"
  },
  "files": [
    "dist",
    "fixtures",
    "README.md"
  ],
  "devDependencies": {
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
