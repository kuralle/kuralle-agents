{
  "name": "@kuralle-agents/upstash-store",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kuralle/kuralle-agents.git",
    "directory": "packages/kuralle-upstash-store"
  },
  "version": "0.8.5",
  "description": "Upstash Vector-backed VectorStore for Kuralle (edge/serverless runtime)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "peerDependencies": {
    "@kuralle-agents/rag": "workspace:*"
  },
  "devDependencies": {
    "@kuralle-agents/rag": "workspace:*",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "test": "bun test ./src/__tests__",
    "clean": "rm -rf dist",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
