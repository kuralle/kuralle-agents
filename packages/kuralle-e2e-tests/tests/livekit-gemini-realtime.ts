#!/usr/bin/env npx tsx
/**
 * @deprecated Test for the retired KuralleGeminiRealtimeModel API contract.
 *
 * Use `agentsession-realtime-authority-gemini-e2e.ts` for the canonical
 * realtime path (createKuralleRealtimeAgent + LiveKit Gemini plugin).
 */
console.warn(
  '\nlivekit-gemini-realtime.ts retired — use agentsession-realtime-authority-gemini-e2e.ts\n',
);
process.exit(0);
