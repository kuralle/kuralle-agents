# Agent Examples (v2)

Parity ports of `line/examples` using v2 authoring (`defineAgent`, `createRuntime`, `defineTool`, handoffs, guardrails).

Run any example:

```bash
cd packages/kuralle-core
bun examples/agents/form-filler.ts
bun examples/agents/basic-chat.ts
```
