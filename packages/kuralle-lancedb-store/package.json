{
  "name": "@kuralle-agents/lancedb-store",
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kuralle/kuralle-agents.git",
    "directory": "packages/kuralle-lancedb-store"
  },
  "version": "0.3.11",
  "description": "LanceDB-backed VectorStore for Kuralle (Node/Bun only, not for edge runtimes)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "peerDependencies": {
    "@kuralle-agents/rag": "workspace:*",
    "@lancedb/lancedb": ">=0.13.0"
  },
  "devDependencies": {
    "@kuralle-agents/rag": "workspace:*",
    "@lancedb/lancedb": "^0.13.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist",
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "test": "bun test test",
    "prepublishOnly": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "files": [
    "dist",
    "README.md"
  ]
}
