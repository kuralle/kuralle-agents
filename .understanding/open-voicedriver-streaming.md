| ID | Question | Would resolve if | Suggested command |
|----|----------|------------------|-------------------|
| O1 | Does Gemini's `outputTranscription.text` overlap with `modelTurn.parts[].text` in `outputAudioTranscription: {}` mode? Could cause double-counting in `TokenSource`. | Run a live Gemini turn with `outputAudioTranscription: {}` and log every `transcript(assistant)` event — check for duplicate text segments. | `bun run packages/kuralle-core/examples/<gemini-transcript-log>.ts` |
| O2 | Should `safety-blocked`/`pipeline-validation-block` events be emitted in `speakGated` (shared path) or in `VoiceDriver` (post-hoc only)? | Decide based on RFC §10 language ("emit the relevant safety-*/pipeline-validation-* events and, on block, trigger the provider interrupt + correction utterance") — the provider interrupt is voice-specific, suggesting VoiceDriver code after `speakGated` returns. | Read RFC §10 again; if ambiguity remains, ask RFC author. |
| O3 | What is the exact mechanism for triggering a provider "correction utterance" after a post-hoc gate block? `requestResponse(correctionText)` or `sendClientContent`? Does it work after `turn-complete` has already fired? | Test: after `applyPostTurnPolicies` returns block, call `this.client.requestResponse(safeMessage)` and verify provider speaks the correction. | `/delegate --mode impl` a small test in `bargein.test.ts` |
