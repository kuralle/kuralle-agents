| ID | Question | Would resolve if | Suggested command |
|----|----------|------------------|-------------------|
| O1 | Should `AgentStreamPart` (`processors.ts:92-102`) be updated in S1-01? It is NOT in the live emit path but defines a public API callback shape that would be inconsistent if left with the old `text-delta.text` while `HarnessStreamPart` gets the new lifecycle. | Manager reads `.understanding/text-delta-consumers.md` §AgentStreamPart verdict and decides | Read `packages/kuralle-core/src/types/processors.ts:92-102`, `packages/kuralle-core/src/types/runtime.ts:25-31` |
| O2 | Should `streamFilter.ts` `SAFE_EVENT_TYPES` include `text-start`, `text-end`, `text-cancel` in addition to `text-delta`? The 'safe' filter controls which events reach external clients. If only `text-delta` is in the safe set, `text-start`/`text-end`/`text-cancel` would be filtered out — possibly correct (they are framing-only). | Manager/RFC author decides visibility of lifecycle events to external clients | Read `streamFilter.ts:2-13`, RFC §5.3 |
| O3 | For the `KuralleRuntimeLLMAdapter` in S1-01 compile-only mode: should the fix be minimal (`.text` → `.delta` only, keep buffered behavior) or full lifecycle handling (ignore start/end, halt on cancel)? The WBS says "compile-only" for S1-01 with true streaming in Sprint 3. | Manager confirms S1-01 scope: compile-only vs. partial handling | Read WBS §Sprint 1 S1-01 description, `KuralleRuntimeLLMAdapter.ts:200-230` |
| O4 | Should `onInterim` (`Runtime.ts:131`) emit a full `text-start`/`text-delta`/`text-end` trio for transient "still working…" messages, or should it emit a bare `text-delta` without lifecycle framing (as a non-turn event)? The RFC doesn't specify interim message behavior explicitly. | Manager + RFC author decide whether interim messages are independent mini-turns or inline deltas | Read `Runtime.ts:128-132`, RFC §4.1 event lifecycle |
