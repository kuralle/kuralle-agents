# S5-02 — broadcast engine + BroadcastLedger (demo)

## Named tests (packages/kuralle-engagement/test/broadcast.test.ts)

- BroadcastLedger.putIfAbsent returns true once then false for the same key
- createBroadcasts.sends template only to opted-in recipients
- broadcast_ledger_idempotent_per_campaign_recipient
- broadcast_reply_enters_flow

## Verification

$ bun test packages/kuralle-engagement/test/broadcast.test.ts
  4 pass, 0 fail

$ bun test packages/kuralle-engagement
  50 pass, 0 fail

$ bun run typecheck:all
  swept 58 configs; 0 stale-empty
  ✓ typecheck:all green
  swept 7 playground configs; 0 stale-empty
  ✓ typecheck:playground green
  (eslint clean)
