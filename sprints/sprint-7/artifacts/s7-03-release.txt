S7-03 — README + docs + publish-together dry-run

=== bun run typecheck:all ===
58 framework tsconfigs + 7 playground configs: all ok
eslint clean
EXIT 0

=== §9 test matrix ===
Note: `bun test packages/kuralle-core packages/kuralle-messaging ...` (single invocation with 4 paths) only discovers core (907 tests). Full matrix run sequentially:

bun test packages/kuralle-core          → 374 pass, 0 fail
bun test packages/kuralle-messaging     → 448 pass, 0 fail
bun test packages/kuralle-messaging-meta → 303 pass, 0 fail
bun test packages/kuralle-engagement    → 85 pass, 0 fail
Total: 1210 pass, 0 fail
EXIT 0

=== scripts/check-no-source-maps.sh ===
✓ no source maps or raw src in any publishable package tarball
EXIT 0

=== find packages/kuralle-engagement/dist packages/kuralle-messaging/dist -name '*.map' ===
(empty — no .map files)
EXIT 0

=== pnpm publish -r --dry-run (repo root) ===
First attempt (dirty tree): ERR_PNPM_GIT_UNCLEAN — expected before commit.
Retry: pnpm publish -r --dry-run --no-git-checks
  → dry-run published @kuralle-agents/engagement@0.2.0 only (new package; core/messaging/messaging-meta already at registry versions).
  → tarball: 49 files, no .map in contents.
EXIT 0

=== pnpm pack @kuralle-agents/engagement (workspace rewrite check) ===
Packed dependencies after workspace:* rewrite:
  @kuralle-agents/core: 0.2.1
  @kuralle-agents/messaging: 0.2.0
  @kuralle-agents/messaging-meta: 0.2.0
No split-graph pin (engagement pins current graph versions, not stale exact core).

=== apps/docs build ===
guides/engagement/index.html built
EXIT 0

=== Version bump ===
@kuralle-agents/engagement: 0.0.0 → 0.2.0 (release-prep for first publish)
